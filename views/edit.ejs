<% const title = 'Edit Post' %>
<%- include('partials/header', { title, currentUser }) %>

<h1>Edit Post</h1>

<% if (error) { %><p class="alert alert-error"><%= error %></p><% } %>

<form action="/posts/<%= post.id %>?_method=PUT" method="POST" class="post-form">
  <label for="post-title">Title</label>
  <input type="text" id="post-title" name="title" value="<%= post.title %>" required />

  <label for="tags">Tags <span class="hint">(comma-separated)</span></label>
  <input type="text" id="tags" name="tags" value="<%= Array.isArray(post.tags) ? post.tags.join(', ') : (post.tags || '') %>" />

  <div class="editor-tabs">
    <button type="button" id="tab-write" class="tab-btn tab-active">Write</button>
    <button type="button" id="tab-preview" class="tab-btn">Preview</button>
  </div>

  <div class="editor-panes">
    <div id="write-pane" class="pane">
      <label for="content">Content <span class="hint">(Markdown supported)</span></label>
      <textarea id="content" name="content" rows="16" required><%= post.content %></textarea>
    </div>
    <div id="preview-pane" class="pane hidden">
      <label>Preview</label>
      <div id="markdown-preview" class="markdown-preview"></div>
    </div>
  </div>

  <label class="checkbox-label">
    <input type="checkbox" name="isPublic" <%= post.isPublic ? 'checked' : '' %> />
    Make this post public (visible to everyone)
  </label>

  <div class="form-actions">
    <button type="submit" class="btn btn-primary">Save Changes</button>
    <a href="/posts/<%= post.id %>" class="btn btn-secondary">Cancel</a>
  </div>
</form>

<script src="https://cdnjs.cloudflare.com/ajax/libs/marked/12.0.2/marked.min.js"></script>
<script src="/js/editor.js"></script>

<%- include('partials/footer') %>
